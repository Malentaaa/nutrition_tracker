## Calorie Tracker — мультиагентная система для учёта питания (ADK + OpenFoodFacts)
Сделано в рамках Kaggle · Community Hackathon · Capstone Project

Этот проект — готовый пример мультиагентной системы, построенной на Google Agent Development Kit (ADK), которая автоматически распознаёт намерение пользователя (добавить продукт, удалить, сбросить дневные данные, показать итоги) и вычисляет калорийность и БЖУ на основе данных OpenFoodFacts.

Система обеспечивает интуитивный диалоговый интерфейс, хранение состояния пользователя, автоматический подсчёт дневной статистики и хранение истории сессии.
___
### 🎯 Постановка задачи

Подсчёт калорий вручную — утомительный и малоподъёмный процесс. Пользователь вынужден:
- искать продукты в интернете;
- вручную считать калории и БЖУ;
- постоянно вести дневник питания;
- помнить, что он ел;
- исправлять ошибки и пересчёты;
- тратить время на то, что должно быть автоматизировано.

Такой подход плохо масштабируется и часто приводит к отказу от контроля питания.
___
### 💡 Заявление о решении

Calorie Tracker, построенный на основе мультиагентной архитектуры ADK:
- распознаёт намерение: добавить, удалить, показать, сбросить питание;
- автоматически вычисляет калории и БЖУ с помощью OpenFoodFacts API;
- обновляет дневной итог в памяти;
- хранит историю и самосохраняет состояние сессии;
- взаимодействует с пользователем естественным языком;
- работает как в ADK Web, так и локально через run_debug.

Это превращает ведение питания в простой диалог:
```
Я: I ate 150 g rice
Агент: +186 kcal, обновлён дневной итог
```
___
### 🏗️ Архитектура проекта

Архитектура построена на базе SequentialAgent, который объединяет два главных подагента:
```
IntentAgent → NutritionAgent → Memory Auto-Save
```

<img width="1466" height="686" alt="chema" src="https://github.com/user-attachments/assets/9295ec69-5223-4372-a726-cc660dabe111" />

___
### 🔧 Основные компоненты
🔹 IntentAgent
Распознаёт намерение пользователя:
- ADD — добавить продукт
- REMOVE — удалить продукт
- TOTALS — показать дневной итог
- RESET — сбросить дневной статистику
Использует модель Gemini (gemini-2.5-flash-lite) для классификации текста и выделения количества/продукта.
___
🔹 NutritionAgent
Отвечает за:
- вычисление калорий и БЖУ;
- вызов функции `calculate_calories`;
- обновление дневных итогов через `update_daily_totals`;
- формирование текстового ответа.
___
🔹 Автоматическое сохранение
```
async def auto_save_to_memory(callback_context):
    """Автоматически сохраняет состояние сессии после каждого шага агента."""
    await callback_context._invocation_context.memory_service.add_session_to_memory(
        callback_context._invocation_context.session
    )
```
После добавления after_agent_callback=auto_save_to_memory агент становится полностью готов к работе в ADK Web.
___
### 📦 Структура проекта
```
nutrition_tracker/
│
├── agent.py              # root_agent + pipeline
├── intent.py            # IntentAgent
├── nutrition.py         # NutritionAgent
├── tools.py             # все функции: search, calculate, update, reset...
├── config.yaml          # (опционально) модель и настройки ADK
└── .env                 # только локальные ключи
```
___
### 🚀 Как запустить локально (ADK Web)
```
adk web
```
### Пример работы ADK
<img width="2829" height="1655" alt="image (4)" src="https://github.com/user-attachments/assets/e038532a-ffbe-48b2-bf0e-591794005f25" />




